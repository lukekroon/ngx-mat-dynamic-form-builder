import { NgxMatFormContentDirective } from './ngx-mat-form-content.directive';

describe('NgxMatFormContentDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMatFormContentDirective();
    expect(directive).toBeTruthy();
  });
});
